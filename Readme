GORM: used for model-based CRUD and simple queries.

Raw SQL: used when GORM can’t express the query (e.g. COUNT(DISTINC) COALESCE, custom aggregations).




# Run all load tests
go test ./tests/load_tests/... -v


Copy the example environment file and update the values if necessary:

cp .env.example .env